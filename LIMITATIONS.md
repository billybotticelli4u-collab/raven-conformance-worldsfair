# Limitations

1. **Deploy freeze** — no CLI/Vercel promote from Billy; Owner git-only YES required.
2. **Public Fair URL** may still be SSO-walled / vanity HTTP 200 landing / API 404 / wrong slice until Owner promote.
3. **Conformance surface does not claim signed evidence** after polish fix.
4. **child_process / sandbox_exec** disclosures apply; not a security sandbox claim.
6. **verify-judge-url against production** may fail closed on identity UNKNOWN until the identity fail-closed gate is deployed (I-2 is merged into the release tip; production deployment remains an Owner decision); HTTPS transport for check #8 is fixed in this tip.
